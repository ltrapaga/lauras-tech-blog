<div class="row">
  {{#each this}}
  <div>
    <p class="card-body-2">
      {{comment_content}}
      <br>
      - {{user.username}}, {{format_date comment_date}}
    </p>
  </div>
  {{/each}}
</div>